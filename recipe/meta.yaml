{% set name = "libcflib" %}
{% set version = "0.0.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/regro/{{ name }}/releases/download/{{ version }}/{{ version }}.tar.gz
  sha256: a6c709d33115fd0ca0a7c43a48c17f96d00ea7f1a3f18b4de679dadc71606c3f

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - python
    - pip
  run:
    - python
    - cerberus
    - tornado
    - whoosh
    - xonsh
    - zict
    - tqdm
    - toolz
    - jinja2
    - lazyasd
    - pandas

test:
  imports:
    - libcflib
    - libcflib.environ

about:
  home: http://github.com/regro/libcflib
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Library Conda Forge Library'

extra:
  recipe-maintainers:
    - scopatz
    - CJ-Wright
    - mariusvniekerk
